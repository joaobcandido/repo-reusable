name: Reusable Global Workflow

on:
  workflow_call:
    inputs:
      anbiente:
        required: true
        type: string

jobs:
  passos:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Executando o Reusable no repo3! Versão recebida -> ${{ inputs.anbiente }}"
